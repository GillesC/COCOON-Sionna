# Figure Assets

Place the concept illustration for the manuscript here as `cocoon_concept.pdf`.

The generated Rabot figure set used by the article is mirrored under `rabot/`, including the PGFPlots `.tex` files, preview `.png` files, and companion `tikz_data/` tables required to compile those figures locally from the `article/` tree.
