# Figure Slot

Place the concept illustration for the manuscript here as:

- `cocoon_concept.pdf`

The LaTeX scaffold references that exact path through `\ConceptFigurePath`.
If the file is absent, the paper will render a boxed placeholder instead of failing.
