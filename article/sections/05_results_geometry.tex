\section{Scenario Geometry and Mobility Results}
\label{sec:results_geometry}

This result block summarizes the simulated environment before presenting radio-performance metrics. It reports the available candidate mounting locations, the selected deployment, and the spatial distribution of user trajectories over the Rabot area.

\begin{figure}[t]
    \centering
    % \begin{subfigure}[t]{0.47\textwidth}
    %     \centering
    %     \PaperImage[\linewidth]{\resultroot/scene_layout.png}
    %     \caption{Top-down Rabot scene layout with candidate sites, selected sites, and simulated mobility overlays.}
    %     \label{fig:scene_layout}
    % \end{subfigure}\hfill
    % \begin{subfigure}[t]{0.47\textwidth}
    %     \centering
    %     \PaperImage[\linewidth]{\resultroot/trajectory_colormap.png}
    %     \caption{Trajectory colormap used to visualize the temporal spread of user motion through the walkable space.}
    %     \label{fig:trajectory_colormap}
    % \end{subfigure}

    \PaperImage[\linewidth]{\resultroot/scene_layout.png}
    \caption{Geometry results for the Rabot scenario. The panels show the selected deployment in the urban layout and the simulated user-trajectory overlay over the walkable area.}
    \label{fig:geometry_pair}
\end{figure}

\Cref{fig:geometry_pair} provides the geometric context for the subsequent \gls{sinr}, \gls{esr}, and relocation analyses. The scene-layout panel reports the candidate-site pool and the selected constellation, whereas the trajectory overlay reports the spatial spread of the simulated user population.
For the present Rabot instance, the wall-generation stage yields \num{87} candidate low-height mounting locations, from which \num{6} distributed sites are activated per distributed strategy. The simulated mobility trace contributes \num{7210} user-snapshot positions over the \SI{1}{\hour} horizon, and these positions span most of the accessible outdoor footprint rather than a single narrow corridor. This geometry already suggests that a single rooftop proxy will face a harder coverage problem than wall-mounted distributed deployments, because the user population repeatedly moves through streets and courtyard edges that can be served from multiple nearby façades.
% \FloatBarrier
